name: device_client
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

  clipboard_watcher: ^0.2.1
  logging: ^1.3.0
  package_info_plus: ^8.1.0
  rxdart: ^0.28.0
  share_plus: ^10.1.1
  shared_preferences: ^2.3.2
